import React from 'react';

const Header = () => {
  return (
    <header className="text-black font-bold bg-offwhite p-4 flex items-center justify-center">
      <h1 className="text-2xl">Analytics Dashboard</h1>
    </header>
  );
};

export default Header;
